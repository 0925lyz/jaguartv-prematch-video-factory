# Consolidation Map

| Source task or project | Reused here | Deliberately excluded |
| --- | --- | --- |
| Current Task 1 window | Operator's conflict-resolution requirements, delivery scope, routing policy | Embedded credentials and stale provider overrides |
| Task 1 pre-match implementation A | Player asset gates, prompt/job manifests, 4:5 poster and 9:16 master rules | Date-specific fixture constants |
| Task 1 pre-match implementation B | DeepSeek routing, current batch evidence records, provider route logging | Absolute run paths and one-off batch scripts |
| Daily fixture collector | Stable fixture records, featured status, channels, source payload and timestamps | Public wording that calls the timezone São Paulo time |
| Poster-only task | Image2 background plus deterministic factual overlay and QA | Unverified player substitutions |
| Pre-match task | Lower prediction panel, readable cover copy, Figure 1 placement | Earlier dense text layouts |
| Post-match task | Evidence schema, credential resolution, validation and upload verification patterns | Results, final-score wording and post-match selection semantics |
| Operator-supplied September 2026 media folders | Operation clips, CTA videos, music, and CTA voice inventory | README, JSON metadata, static CTA images, and removed legacy assets |
| `jaguar视频二创` | Pending Review import contract and saved CTA inventory for delivery | Repository source modifications |

The standalone repository does not import code from the original workspace at runtime. Player and
kit assets remain in the licensed external `image2数据库` because their per-run identity, season,
source, and license evidence must be revalidated before use.
